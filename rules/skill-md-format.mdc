---
description: SKILL.md 格式规范。编辑或创建 SKILL.md 时遵循。
alwaysApply: false
globs: "**/SKILL.md"
---

# SKILL.md 格式规范

## YAML Frontmatter（必需）

文件必须以 `---` 包裹的 YAML 元数据开头：

```markdown
---
name: my-skill
description: 一句话描述该 Skill 的用途
version: 1.0.0
author: 作者名
tags: [标签1, 标签2]
---
```

`name` 必须匹配 `^[a-z][a-z0-9-]*$`。

## 章节要求

评分系统通过 `##` 二级标题中的**中文关键词**识别章节（如"概述"、"核心能力"等）。emoji 前缀（📋、🎯 等）是推荐但非必需的装饰。

以下 5 个章节分为两层：
- **L1（结构合法）**：缺少会导致 validate 报 warning、docs 维度显著失分
- **L2（高分优化）**：有助于提升 docs 和 content 维度评分，推荐但不影响 validate 通过

编写顺序为推荐顺序，非强制：

### 1. 概述（关键词：概述）— L1

```markdown
## Skill 概述

**技能名称**：`my-skill`
**用途**：一句话描述

**解决的问题**：具体描述 Skill 解决的痛点

**目标用户**：谁会使用

**适用场景**：
- 场景描述 1（10-80 字，具体可操作，与 description 语义相关）
- 场景描述 2
- 场景描述 3
```

适用场景列表 ≥ 3 项。评分器检测列表项与 frontmatter `description` 的 2-gram 覆盖率，覆盖率 < 20% 扣 5 分。

### 2. 核心能力（关键词：核心能力）— L1

```markdown
## 核心能力

### 主要功能
- 功能点 1：具体说明（与 description 语义相关）
- 功能点 2：具体说明

### 错误处理
- 场景 → 处理方式
```

列表项应各不相同，不要用"功能点 1"、"能力 1"等占位符。

### 3. 使用方式（关键词：使用方式）— L1

```markdown
## 使用方式

\`\`\`bash
python run.py <command> [options]
\`\`\`
```

### 4. 示例（关键词：示例）— L2

```markdown
## 示例

\`\`\`bash
python run.py scan --path /var/log/app.log
\`\`\`

输出：
\`\`\`
✅ 已扫描 128 条记录
\`\`\`
```

### 5. 故障排除（关键词：故障排除）— L2

表格形式可额外加分：

```markdown
## 故障排除

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 文件未找到 | 路径不存在 | 检查路径拼写 |
```
