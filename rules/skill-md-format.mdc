---
description: SKILL.md 格式规范。编辑或创建 SKILL.md 时遵循此规则。
alwaysApply: false
globs: "**/SKILL.md"
---

# SKILL.md 格式规范

## YAML Frontmatter（必需）

文件必须以 `---` 包裹的 YAML 元数据开头：

```markdown
---
name: my-skill
description: 一句话描述该 Skill 的用途
version: 1.0.0
author: 作者名
tags: [标签1, 标签2]
---
```

- `name` 必须匹配 `^[a-z][a-z0-9-]*$`
- frontmatter 区域必须有至少 2 个 `---` 分隔符

## 必需章节（中文标题，评分系统按此识别）

按以下顺序编写，使用 `##` 二级标题：

### 1. Skill 概述
```markdown
## 📋 Skill 概述

**技能名称**：`my-skill`
**用途**：一句话描述

**解决的问题**：具体描述该 Skill 解决的痛点

**目标用户**：谁会使用

**适用场景**：
- 场景描述 1（10-80 字，具体可操作）
- 场景描述 2
- 场景描述 3
```

### 2. 核心能力
```markdown
## 🎯 核心能力

### 主要功能
- 功能点 1：具体说明
- 功能点 2：具体说明

### 错误处理
- 场景 → 处理方式
```

### 3. 使用方式
```markdown
## 🚀 使用方式

\`\`\`bash
python run.py <command> [options]
\`\`\`
```

### 4. 示例
```markdown
## 📖 示例

\`\`\`bash
python run.py scan --path /var/log/app.log
\`\`\`
```

### 5. 故障排除（表格形式加分）
```markdown
## ❓ 故障排除

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 文件未找到 | 路径不存在 | 检查路径拼写 |
```

## 评分关键点

- **内容相关性**（docs 维度）：适用场景和核心能力的列表项必须与 frontmatter 的 `description` 语义相关。评分器用 2-gram 覆盖率检测，覆盖率 < 20% 会扣 5 分
- **占位符残留**（content 维度）：不要保留模板占位符（如"场景1"、"功能点1"、"TODO"、"your_xxx_here"、"CHANGE_ME"）
- **列表多样性**（content 维度）：适用场景和核心能力下的列表项应各不相同，避免重复表述
- **模板保留率**（content 维度）：如果与模板原文重合度 > 70%，content 维度封顶 5/20 分。必须用实际内容替换模板文本
